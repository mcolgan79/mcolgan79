node_modules/
dist/
coverage/

# Tauri build output & generated files
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock
# icons/ are generated by `npx tauri icon` but committed so a fresh clone
# builds without that step (tauri.conf.json references them at build time)

.DS_Store
*.local
*.tsbuildinfo
